# Currency Commands

## Common Commands

- `stateset "convert 100 USD to EUR"`
- `stateset "list exchange rates"`
- `stateset --apply "set exchange rate USD EUR 0.92"`
- `stateset --apply "set base currency USD"`

## Notes

- Use correct decimal precision per currency.
