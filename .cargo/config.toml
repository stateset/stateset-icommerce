# Build configuration for StateSet iCommerce.
#
# Do not set target-cpu=native globally. Shared CI caches can be restored on
# hosts with different CPU features, which can make rustc/clippy load cached
# artifacts that use unsupported instructions. For local CPU-specific release
# builds, set RUSTFLAGS="-C target-cpu=native" explicitly for that command.
