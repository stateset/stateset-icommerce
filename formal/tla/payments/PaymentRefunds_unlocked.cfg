\* A code path that forgot the lock: the read and the insert are separate
\* steps. TLC must find an over-refund here -- scripts/check.sh fails if it
\* does not, because a model that cannot fail proves nothing.
CONSTANTS
    Amount = 3
    MaxRefunds = 3
    Workers = {w1, w2}
    Locked = FALSE
SPECIFICATION Spec
INVARIANT NoOverRefund
PROPERTIES TransitionsLegal
CHECK_DEADLOCK FALSE
