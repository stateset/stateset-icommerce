[package]
name = "stateset-embedded-php"
version = "1.27.0"
edition = "2021"
description = "PHP bindings for StateSet Embedded Commerce"
license = "MIT OR Apache-2.0"
repository = "https://github.com/stateset/stateset-icommerce"
keywords = ["commerce", "ecommerce", "php", "woocommerce"]
categories = ["api-bindings"]

[lib]
name = "stateset_embedded"
crate-type = ["cdylib"]
doc = false

[features]
default = []
runtime = [
    "dep:chrono",
    "dep:ext-php-rs",
    "dep:rust_decimal",
    "dep:stateset-core",
    "dep:stateset-embedded",
    "dep:stateset-crypto",
    "dep:serde_json",
]

[dependencies]
stateset-embedded = { path = "../../crates/stateset-embedded", package = "stateset-embedded", version = "1.27.0", optional = true }
stateset-core = { path = "../../crates/stateset-core", version = "1.27.0", optional = true }
stateset-crypto = { path = "../../crates/stateset-crypto", version = "1.27.0", optional = true }
ext-php-rs = { version = "0.13", optional = true }
rust_decimal = { version = "1.36", features = ["serde"], optional = true }
chrono = { version = "0.4", features = ["serde"], optional = true }
serde_json = { version = "1.0", optional = true }

[workspace]
