[package]
name = "stateset-kotlin"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Kotlin JNI bindings for StateSet Embedded Commerce"

[lib]
name = "stateset_kotlin"
crate-type = ["cdylib"]

[dependencies]
stateset-embedded = { path = "../../crates/stateset-embedded", version = "1.0.4" }
stateset-core = { path = "../../crates/stateset-core", version = "1.0.4" }
stateset-crypto = { path = "../../crates/stateset-crypto", version = "1.0.4" }

# JNI bindings
jni = "0.21"

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Utilities
uuid = { workspace = true }
rust_decimal = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
jni = "0.21"

[lints]
workspace = true
