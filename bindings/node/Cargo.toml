[package]
name = "stateset-embedded-node"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Node.js bindings for StateSet Embedded Commerce"

[lib]
crate-type = ["cdylib"]

[dependencies]
# napi-rs for Node.js bindings
napi = { version = "2", features = ["async", "napi9", "serde-json"] }
napi-derive = "3"

# Our commerce library
stateset-embedded = { path = "../../crates/stateset-embedded", features = ["vector"] }
stateset-core = { path = "../../crates/stateset-core", features = ["embeddings"] }
stateset-db = { path = "../../crates/stateset-db", features = ["vector"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Decimal handling
rust_decimal = { version = "1.36", features = ["serde"] }

# UUID handling
uuid = { version = "1", features = ["v4"] }

# DateTime handling
chrono = { version = "0.4", features = ["serde"] }

# Async runtime
tokio = { version = "1", features = ["rt-multi-thread"] }

[build-dependencies]
napi-build = "2"

[profile.release]
lto = false
opt-level = 3
